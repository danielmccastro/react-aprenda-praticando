import { render, screen } from "@testing-library/react";
import ListaLivros from "./ListaLivros";

/* test("Renderização do link para Learn React", () => {
  render(<ListaLivros />);
  const linkElement = screen.getByText(/learn react/i);
  expect(linkElement).toBeInTheDocument();
});
 */

/* test("Renderização da string save to reload", () => {
  render(<ListaLivros />);
  const stringTxt = screen.getByText(/save to reload/);
  expect(stringTxt).toBeInTheDocument();
}); */

/* describe("Teste de soma", () => {
  function soma(a, b) {
    return a + b
  };
  test("O resultado deve ser 5", () => {
    expect(soma(2, 3)).toBe(5)
  });
}); */

/* describe("Teste de presenca de um item em array", () => {
  const NOMES = ["Mauricio", "Maujor", "Samy", "Silva"];
  test("Testa a presenca do item 'Maujor' no array NOMES", () => {
    expect(NOMES).toContain("Maujor")
  })
}) */